version: 1.0
runtime: nodejs20
build:
  commands:
    build:
      - echo "Installing dependencies"
      - npm install
      - echo "Building application"
      - npm run build
run:
  runtime-version: 20
  command: npm run start
  network:
    port: 5000
    env:
      PORT: 5000
      NODE_ENV: production